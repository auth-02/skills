# Inspection — <slug>

_Raw output of read-only exploration probes. For anything that requires real-world
data (bucket contents, DB schema, external API shape, file layouts), write a
read-only probe, run it, and paste the result here. Never guess what remote state
looks like — record what you actually observed, with the command that produced it._

## Probe: <what you're checking>
**Command / query:**
```
<the exact command, script, or query run>
```

**Output:**
```
<raw output — trimmed to what's relevant>
```

**Observation:**
- <what this tells us; ⚠️ mark surprises that invalidate an assumption>

---

## Probe: <next check>
**Command / query:**
```

```

**Output:**
```

```

**Observation:**
-
